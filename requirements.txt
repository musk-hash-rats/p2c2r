# Runtime dependencies for ML and advanced features
numpy>=1.24.0  # For ML predictions and task splitting
scikit-learn>=1.3.0  # For production ML models (GradientBoosting, etc.)
pygame>=2.5.0  # For interactive demo (ray tracing game)

# Network dependencies for distributed system
websockets>=12.0  # For WebSocket communication between peers, cloud, and users
psutil>=5.9.0  # For system resource monitoring (CPU, memory, etc.)

# Development dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0
isort>=5.12.0
